import { RiNpmjsFill } from "react-icons/ri";

export function NpmBrandIcon(props: { size?: number }) {
  return <RiNpmjsFill aria-label="npm package" className="shrink-0 text-[#cb3837]" role="img" size={props.size ?? 14} />;
}
